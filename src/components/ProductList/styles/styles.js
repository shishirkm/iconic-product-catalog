import styled from 'styled-components';
// import { colours, spacing } from '../../theme';

const SC = {
    Wrapper: styled.div`
      
    `,
    List: styled.div`
        display: flex;
        flex-wrap: wrap;
    `
};

export default SC;